
services:
  # scraper-startech:
  #   build: .
  #   restart: on-failure
  #   environment:
  #     - TARGET_DOMAIN=startech.com.bd
  #     - SELLER_NAME=StarTech
  #     - CONCURRENCY=5
  #     - DELAY=1.2
  #     - MAX_DUPLICATES=1000
  #     - MAX_ITEMS=100000  
  #   volumes:
  #     - ./data:/app/data

  # scraper-ryans:
  #   build: .
  #   restart: on-failure
  #   environment:
  #     - TARGET_DOMAIN=ryans.com
  #     - SELLER_NAME=Ryans
  #     - CONCURRENCY=5
  #     - DELAY=1.1
  #     - MAX_DUPLICATES=1000
  #   volumes:
  #     - ./data:/app/data
  scraper-lira:
    build: .
    restart: on-failure
    environment:
      - TARGET_DOMAIN=liraimportltd.com
      - SELLER_NAME=Lira
      - CONCURRENCY=5
      - DELAY=1.1
      - MAX_DUPLICATES=1000
      - MAX_ITEMS=1000000
    volumes:
      - ./data:/app/data